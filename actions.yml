name: "UpdateModuleVersion"

description: "Updates a github repo (PS Module or not) version"

inputs:
  vertype: 
    description: "From where the version was optained."
    required: true

  newver:
    description: "updated version"
    required: true

  verbose:
    description: "True to display verbose info during processing"
    required: true
    default: 'false'
        
branding:
  color: blue
  icon: terminal

